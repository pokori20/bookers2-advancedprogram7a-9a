<%= render 'users/list', book: @book, user: @user %>

<h1>Books</h1>
<table>
  <thead>
    <tr>
      <th>image</th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
      <td></td>
      <td><%= link_to book.title, book_path(book) %></td>
      <td><%= book.body %></td>
      <td></td>
      <td></td>
      </tr>

    <% end %>
  </tbody>
</table>
